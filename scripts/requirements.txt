# Python dependencies for the release risk-analysis pipeline scripts.
# jsonschema is used to validate the LLM result against schema/analysis.schema.json.
jsonschema>=4.0.0
# anthropic is required only for the live LLM analysis stage (Claude Opus 4.8).
# The analyze step degrades to an evidence-only fallback when it is absent.
anthropic>=0.39.0
# mcp powers the evidence-collection & analysis tool server the agent drives.
# The server also has a dependency-free stdio fallback, so this is best-effort.
# Pinned >= 1.23.0 to avoid known MCP SDK advisories (DoS / DNS-rebinding).
mcp>=1.23.0
